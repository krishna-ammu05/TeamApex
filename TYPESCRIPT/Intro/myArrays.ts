const fruits:string []=[]
const fruitCount:Array<number>=[]
type User ={
    name:string
    isActive :boolean
}

const allUsers :User[]=[]

const MLModels : number [][]=[
    [255,255,255],
    []
]
console.log(fruits)
fruits.push("Mango")
fruitCount.push(3)

export{}
